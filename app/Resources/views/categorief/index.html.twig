{% extends 'base.html.twig' %}

{% block body %}
<center>
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-40 col-lg-30">
                <div class="login-content">
    <h1>Categoriefs list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Nomc</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for categorieF in categorieFs %}
            <tr>
                <td><a href="{{ path('categorief_show', { 'id': categorieF.id }) }}">{{ categorieF.id }}</a></td>
                <td>{{ categorieF.nomC }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('categorief_show', { 'id': categorieF.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('categorief_edit', { 'id': categorieF.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('categorief_new') }}">Create a new categorieF</a>
        </li>
    </ul>
                </div>
            </div>
        </div>
    </div>
</center>
{% endblock %}
