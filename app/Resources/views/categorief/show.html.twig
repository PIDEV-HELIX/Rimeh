{% extends 'base.html.twig' %}

{% block body %}
    <center>
        <div class="container">
            <div class="row justify-content-center">
                <div class="col-40 col-lg-30">
                    <div class="login-content">
    <h1>Categorief</h1>

    <table border="2">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorieF.id }}</td>
            </tr>
            <tr>
                <th>Nomc</th>
                <td>{{ categorieF.nomC }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('categorief_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('categorief_edit', { 'id': categorieF.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
                </div>
            </div>
        </div>
    </div>
</center>
{% endblock %}
