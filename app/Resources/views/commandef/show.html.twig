{% extends 'base.html.twig' %}

{% block body %}
<center>
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-40 col-lg-30">
                <div class="login-content">
    <h1>Commandef</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commandeF.id }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ commandeF.quantite }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ commandeF.date }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('commandef_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('commandef_edit', { 'id': commandeF.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
                </div>
            </div>
        </div>
    </div>
</center>
{% endblock %}
