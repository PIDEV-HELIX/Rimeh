{% extends 'base.html.twig' %}

{% block body %}
<center>
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-40 col-lg-30">
                <div class="login-content">

    <h1>Commandefs list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Quantite</th>
                <th>Date</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for commandeF in commandeFs %}
            <tr>
                <td><a href="{{ path('commandef_show', { 'id': commandeF.id }) }}">{{ commandeF.id }}</a></td>
                <td>{{ commandeF.quantite }}</td>
                <td>{{ commandeF.date }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('commandef_show', { 'id': commandeF.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('commandef_edit', { 'id': commandeF.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('commandef_new') }}">Create a new commandeF</a>
        </li>
    </ul>

                </div>
            </div>
        </div>
    </div>
</center>
{% endblock %}
